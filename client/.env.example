# URL of the backend API
# Local development (used by `vite dev`):
VITE_API_URL=http://localhost:5000

# Production (used by `vite build` — create .env.production or set in Vercel):
# VITE_API_URL=https://impactpulse-backend.onrender.com
